{
  "name": "cricket-broadcast",
  "version": "1.0.0",
  "private": true,
  "description": "Hop 2 broadcast UI. Consumes cricket-scoring ScoreSnapshot only — two hops from cricket-protocol.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "start": "python3 -m http.server 5173 --directory public"
  }
}
